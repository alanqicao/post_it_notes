<h1> Notes </h1>
<p><%= link_to 'New Page', new_note_path %></p>
<% @notes.each do |note| %>
  <h2><%= note.title %></h2>
  <%= link_to 'Show note', note_path(note) %>
  <%= link_to 'Edit page', edit_note_path(note) %>
  <%= link_to 'Destroy Page', note, method: :delete %>
<% end %>

